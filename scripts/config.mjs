/**
 * Shared campaign configuration for all analytics scripts.
 * CAMPAIGN_START is the first day of real traffic — data before this date
 * is excluded from all reports (development/testing noise).
 */
export const CAMPAIGN_START = "2026-03-20";
